<% provide(:title, "Student Select") %>
  <h2>
    Select a Student to Play
  </h2>


                                           
  <table class="table table-striped table-bordered table-hover table-condensed">
    <thead>
      <tr>
        <th>StudentID</th>

      </tr>
    </thead>
    <tbody>
        <% @student.each do |student| %>
		<% if student.active? %>
        
          <tr><td><%= student.name %></td><td><%= link_to "Play   ", chapter_four_path, class: 'btn btn-success btn-xs'  %></td></tr>
        
		<% end %>
        <% end %>
      </ul>
    </tbody>
  </table>
</div>


</body>
</html>