module DashboardHelper
   def scorename(integer)
	student = Student.find_by(id:integer)
	return student.name
   end

   def recentscorename
        student = Student.find_by(id:@recent.student_id)
	return student.name
   end
end
